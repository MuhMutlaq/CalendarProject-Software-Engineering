# Core FastAPI dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Date and time parsing
python-dateutil==2.8.2

# PDF processing
PyPDF2==3.0.1

# Environment variables
python-dotenv==1.0.0

# HTTP requests (for Gemini API)
requests==2.31.0

# Optional: OCR (not needed for Gemini vision API, but available if needed)
# Pillow==10.1.0
# easyocr==1.7.0
# pdf2image==1.16.3
# numpy==1.24.3
# opencv-python==4.8.1.78
# opencv-contrib-python==4.8.1.78

# Note: We use Gemini API via requests, not google-genai SDK
# The API key is configured via environment variables
